```
4) Используя утилиту grep и регулярные выражения напишите в файле user.job
команду задания, выводящего отсортированный список уникальных адресов
электронной почты в файл allemails.lst.
5) Поставьте на выполнение это задание (user.job) через одну минуту с помощью
at.

at -f user.job now+1 minutes

grep -Eioh '(^|\:|\(|[[:blank:]])[[:alpha:]]{1}[[:alnum:].-]+@[[:alnum:].-]+[[:alpha:]]{1}($|[[:blank:]]|\))' ~/hw/lab2/* | grep -Ev '(\.\-|\-\.|\.\.|\-\-)'


grep -Eh '.*@.*' ~/hw/lab2/* | sort | uniq

[:blank:] - Включает символы пробела и табуляции
[:alnum:] - Алфавитно-цифровые символы; эквивалент диапазона [A-Za-z0-9] в ASCII
[:alpha:] - Алфавитные символы; эквивалент диапазона [A-Za-z] в ASCII

Опережающая проверка
Синтаксис опережающей проверки: X(?=Y).Он означает: найди X при условии, что за ним следует Y. Вместо X и Y здесь может быть любой шаблон.

find . -name "*.txt" -exec grep -Eioh '(^|\:|\(|[[:blank:]])[[:alpha:]]{1}[[:alnum:].-]+@[[:alnum:].-]+[[:alpha:]]{1}($|[[:blank:]]|\))' {} \; | grep -Ev '(\.\-|\-\.|\.\.|\-\-)' | grep -Eio '[[:alnum:]].*[[:alnum:]]' | tr [:upper:] [:lower:] | sort -u > base.txt

sed 's/\([[:blank:]]\)*/\1/g' ./textI.txt

echo "hello    world" |  sed 's/\([[:blank:]]\)*/\1/g'

```
